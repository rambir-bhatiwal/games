// Neon Horizon: Cosmic Dreamworld
// Audio Synthesis System
// Real-time sound effects and engine harmonics synthesized via native Web Audio API in game.js

if (typeof window !== 'undefined') {
    window.NEON_AUDIO_LOADED = true;
}
